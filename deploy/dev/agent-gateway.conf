events {}

http {
    server {
        listen 19443 ssl;
        server_name _;
        ssl_certificate /etc/xkp/agent-tls/server.crt;
        ssl_certificate_key /etc/xkp/agent-tls/server.key;
        ssl_protocols TLSv1.2 TLSv1.3;

        location = / {
            default_type text/plain;
            return 200 "XKP5 Agent gateway online\n";
        }

        location = /health {
            proxy_pass http://host.docker.internal:19243/health;
            proxy_set_header Host $host;
        }

        location /agent/v1/ {
            client_max_body_size 1m;
            proxy_pass http://host.docker.internal:19243/agent/v1/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }

        location /files/ {
            proxy_pass http://host.docker.internal:19243/files/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }
    }
}
