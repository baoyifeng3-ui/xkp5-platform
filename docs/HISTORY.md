# 重大变更

## 2026-09-11 版本 5.1.0

冻结本轮镜像、Agent、实训/比赛容器切换、课堂接管、登录背景蒙版及相关部署修复。Agent 版本 `0.2.34`，提交 `e6539e1`。平台提交、镜像摘要和离线包校验写入最终发布清单。

## 2026-09-11 登录背景蒙版透明度

平台设置新增登录背景蒙版透明度（0–100%，默认35%），背景图片层保持不透明。覆盖新旧管理入口、API、键值数据库、全局状态和登录页渲染。部署 Java/Vue `background-overlay-20260911`；42%真实验证后恢复35%，删除此前误建的图片透明度键。

## 2026-09-10 统一切换后续修复

部署 Java `switch-20260910c`、Vue `switch-20260910b`。补齐教师接管、课堂版本同步、真实启动阶段、下课及模式过渡禁用，统一比赛离线预检查为30秒。Java254项、前端15脚本、MySQL9项通过，真实课程接管与离线拒绝通过。代码浏览器因新旧CA信任不一致仍待处理，不标记完整验收。详见 `UNIFIED_SWITCH_QA_2026-09-10.md`。

## 2026-09-10 实训与模式联合修复

部署 Java `qa-fixes-20260910d`、Vue `qa-fixes-20260910c`、Agent `0.2.34`。
修复镜像唯一计数、升级资格、证书安装、创建状态回显、遗留无步骤模式任务、失败回报空值与丢失、独立环境目录隔离、课堂停止和容器切换边界、单标注环境冷启动。
Java 264 项、Vue 15 个脚本、Linux Agent 6 包、MySQL 9 项通过；真实双服务器容器与课堂/比赛往返通过。
报告：`ENVIRONMENT_MODE_QA_2026-09-10.md`。临时环境已清理，原有 8 个比赛环境保留，平台恢复实训模式。
