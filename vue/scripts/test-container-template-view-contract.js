const assert = require('assert')
const fs = require('fs')
const navigation = require('../src/navigation/roleNavigation')

assert.ok(navigation.operationsItems.some(item => item.route === '/operations/container-templates'))
const router = fs.readFileSync('src/router/index.js', 'utf8')
const view = fs.readFileSync('src/views/operations/ContainerTemplates.vue', 'utf8')
const api = fs.readFileSync('src/api/ContainerTemplates.js', 'utf8')
assert.ok(router.includes("path: 'container-templates'"))
assert.ok(view.includes('publishContainerTemplate'))
assert.ok(view.includes('disableContainerTemplate'))
assert.ok(view.includes('GPU 算力'))
assert.ok(api.includes('/super-admin/container-templates'))
assert.ok(api.includes('/disable'))
console.log('container template view contract tests passed')
