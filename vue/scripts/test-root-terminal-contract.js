const assert = require('assert')
const fs = require('fs')

const read = path => fs.readFileSync(path, 'utf8')
const api = read('src/api/TerminalSessions.js')
const service = read('src/services/rootTerminalSession.js')
const dialog = read('src/components/agents/RootTerminalDialog.vue')
const agents = read('src/views/operations/ProcessingAgents.vue')

assert.ok(api.includes('/operations/processing-agents/${agentId}/terminal-sessions'))
assert.ok(api.includes('/operations/terminal-sessions/${sessionId}/browser-ticket'))
assert.ok(api.includes('request.delete'))
assert.ok(service.includes('xkp-terminal-v1'))
assert.ok(service.includes('xkp-terminal-ticket.${ticket}'))
assert.ok(service.includes("status.state === 'WAITING_AGENT'"))
assert.ok(service.includes('getTerminalSession(sessionId)'))
assert.ok(service.includes('AbortSignal') || service.includes('options.signal'))
assert.ok(service.includes('TERMINAL_CONNECTION_TIMEOUT'))
assert.ok(service.includes("binaryType = 'arraybuffer'"))
assert.ok(!service.includes('console.log'))
assert.ok(!service.includes('ticket=${ticket}'))
assert.ok(!service.includes('localStorage'))
assert.ok(dialog.includes('xterm'))
assert.ok(dialog.includes('xterm-addon-fit'))
assert.ok(dialog.includes("xterm/css/xterm.css"))
assert.ok(dialog.includes('beforeDestroy'))
assert.ok(dialog.includes('connectAbort.abort()'))
assert.ok(service.includes('closeTerminalSession'))
assert.ok(dialog.includes('WAITING_AGENT'))
assert.ok(dialog.includes('absoluteExpiresAt'))
assert.ok(dialog.includes('agentConnectionDeadline'))
assert.ok(dialog.includes("this.state === 'WAITING_AGENT'"))
assert.ok(dialog.includes('FAILED'))
assert.ok(dialog.includes('CLOSING'))
assert.ok(agents.includes('RootTerminalDialog'))
assert.ok(agents.includes('openRootTerminal'))
assert.ok(api.includes('OPEN_ROOT_TERMINAL'))
assert.ok(agents.includes('selected.online'))
assert.ok(agents.includes('selected.enabled'))
assert.ok(agents.includes('SUPER_ADMIN'))
assert.ok(agents.includes('terminalOpening'))
assert.ok(service.includes('Math.max(80'))
assert.ok(service.includes('Math.min(300'))
assert.ok(service.includes('Math.max(24'))
assert.ok(service.includes('Math.min(100'))
assert.ok(service.includes("type: 'resize'"))
assert.ok(service.includes('closeTerminalSession(sessionId)'))
console.log('root terminal contract tests passed')
