# 项目协作规则

- 修改前先核对问题前提、依赖和实际调用路径。
- 区分已确认事实、推测和待验证事项；涉及数字、人物和结论时尽量核实来源。
- 优先复用现有脚本、配置和项目约定，避免无必要的抽象和依赖。
- 不记录密码、连接串、私钥或其他敏感数据。
- 仅读取与当前任务直接相关的文档；不要默认读取历史流水。
- 产生持久变化后，更新 `TODO.md` 当前状态；重大变更再追加到历史记录。
- 镜像清单按完整镜像 ID 计数，标签是同一镜像的别名；删除前保留模板及容器引用检查。
- Agent 升级目标及资格由后端统一提供；前后端不得用版本不相等代替高低比较，包版本必须与 Agent 源码版本一致。
- 同机环境命令逐条租赁、执行并持久化结果，成功回报前不执行下一条；模式切换保持先停后启。
- 环境已提交不等于创建或启动完成；验收需核对 Agent 回报、数据库与真实 Docker/工具服务。
- 代码编辑器验收必须包含学生浏览器中的加载、WebSocket及保存；指定 CA 的服务端 HTTP 检查不能代替浏览器验收。更新平台根 CA 时必须同步处理客户端信任，不能只保留相同证书名称。
